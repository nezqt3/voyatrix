BOT_TOKEN=put-your-telegram-bot-token-here
CSV_DIR=aggregation/csv
MEDIA_ROOT=aggregation/export

# Leave WEBHOOK_BASE_URL empty for local polling. Render supplies its URL automatically.
# WEBHOOK_BASE_URL=https://your-service.example.com
# Use only A-Z, a-z, 0-9, underscore and hyphen (1-256 characters).
# WEBHOOK_SECRET=replace-with-a-long-random-secret
